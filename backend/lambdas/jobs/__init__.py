"""
Jobs Lambdas — listing endpoints and on-demand harvest triggers.

Main module: `list_jobs.py` (API Gateway → DynamoDB domain tables;
empty tables can trigger SSM harvest on the EC2 harvester).
"""
